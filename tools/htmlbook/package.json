{
  "name": "onemath-htmlbook",
  "private": true,
  "description": "KaTeX renderer for the One Math Book HTML converter. The katex version MUST match the one pinned in the saas repo (the pre-rendered markup and the served CSS must agree).",
  "type": "module",
  "dependencies": {
    "katex": "0.18.1"
  }
}
