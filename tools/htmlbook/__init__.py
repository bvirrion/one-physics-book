# htmlbook — LaTeX -> HTML converter for the One Math Book online reader.
#
# Scope: exactly the constructs the book sources use (see CONTRIBUTING.md).
# The converter hard-errors on anything it does not recognize, so the HTML
# can never silently drift from the book.
