% !TEX program = lualatex
% ============================================================================
% One Physics Book — الفيزياء الجامعية — السنة الثالثة
% Arabic edition of one_physics_book_5_university_year_3.tex.
%
% Build:  latexmk one_physics_book_5_university_year_3_ar.tex
% Output: build/one_physics_book_5_university_year_3_ar.pdf
% ============================================================================
\documentclass[11pt,a4paper,twoside,openany]{book}

\newcommand{\booklang}{ar}
\usepackage{styles/onephysics}

% Arabic sets no hyphenation patterns, and babel's bidi=basic boxes every
% inline $...$ so interword glue barely stretches: without this, a paragraph
% dense in formulae can have no legal break point at all. Book 2's
% ar/hi/es/pt/id entries and Book 4's Arabic entry carry the same line; a
% Year 3 volume is denser in formulae than either, so it lives here from the
% start rather than as local overrides inside the chapter bodies.
\setlength{\emergencystretch}{3em}

% Brand the running header: rosette + ONE-COURSE.COM (linking to the site)
% above the chapter mark and the book title, on even pages; page number at the
% foot. See \ombrandheader in styles/onephysics.sty.
\ombrandheader

% Print, at the end of every exercise and weekend problem, a hyperlinked
% "Solution p. N" pointer to its solution in the end-of-book appendix.
\omsolutionlinks

% Version and date default to empty / today, but a release build supplies a
% generated version.tex that overrides them (see .github/workflows/release.yml).
\newcommand{\bookdate}{\today}
\newcommand{\bookversion}{}
\IfFileExists{version.tex}{\input{version.tex}}{}

% This volume's line on the shared cover (frontmatter/titlepage.tex).
\newcommand{\bookline}{الكتاب 5: الفيزياء الجامعية — السنة الثالثة}

\title{One Physics Book}
\author{المساهمون في One Physics Book}
\date{\bookdate}

\begin{document}

\frontmatter
\input{frontmatter/titlepage}
\input{frontmatter/colophon}
\input{frontmatter/preface.ar}
\input{frontmatter/image-credits-book5.ar}
\tableofcontents

\mainmatter

% --------------------------------------------------------------------------
% Parts — one per year.
% --------------------------------------------------------------------------
\input{parts/bachelor-3/part}

% --------------------------------------------------------------------------
% Solutions to all exercises
% --------------------------------------------------------------------------
\appendix
\part{\omsolutionspart}
\input{parts/bachelor-3/solutions/solutions}

\backmatter
\printindex

\end{document}
