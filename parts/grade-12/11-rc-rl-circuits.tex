\chapter{RC and RL Circuits}\label{ch:g12:rc-rl-circuits}

Press a camera's shutter halfway: a \omterm{def:g11:lenses-and-eye:lens}{thin} whine climbs for two seconds, a
light turns green --- only then will the flash fire, dumping in a millisecond
what it spent two seconds gathering. This chapter meets the two components
that give circuits a memory and a clock --- \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} and \omterm{def:g11:magnetic-fields:solenoid}{coil} --- and
physics' first differential equations, checkable with this year's derivatives.

\section{The capacitor}

\begin{definition}[Capacitor and capacitance]\label{def:g12:rc-rl-circuits:capacitor}
A \emph{capacitor}\index{capacitor} is a pair of facing metal plates
separated by a \omterm{def:g11:lenses-and-eye:lens}{thin} insulator. Wired to a source, the plates take
opposite charges $+q$ and $-q$ --- an \omterm{def:g11:electric-gravitational-fields:efield}{electric field} fills the gap
(\cref{ch:g11:electric-gravitational-fields}) --- and the \omterm{def:g11:circuits-and-power:voltage}{voltage} $u$
across the plates is proportional to the stored charge:
\[ q = C\,u . \]
The constant $C$ is the \emph{capacitance}\index{capacitance}, in
\emph{farads}\index{farad} (\unit{F}): $\qty{1}{F} = \qty{1}{C/V}$.
\end{definition}

\begin{example}[Orders of magnitude]\label{ex:g12:rc-rl-circuits:orders}
One \omterm{def:g12:rc-rl-circuits:capacitor}{farad} is enormous --- one \omterm{def:g11:fundamental-interactions:charge}{coulomb} parked per \omterm{def:g11:circuits-and-power:voltage}{volt}. Ceramics of radio
circuits: \unit{pF} to \unit{nF}; electrolytics of \omterm{def:g11:work-of-force:power}{power} supplies and
flashes: \unit{\micro F} to \unit{mF}; supercapacitors: thousands of \omterm{def:g12:rc-rl-circuits:capacitor}{farads}.
\end{example}

\begin{proposition}[Current into a capacitor]\label{prop:g12:rc-rl-circuits:cdudt}
The current arriving on a \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}'s plate is the rate at which its
charge grows:
\[
i = \frac{dq}{dt} = C\,\frac{du}{dt} ,
\]
lowercase marking time-varying quantities ($U$, $I$ stay steady,
\cref{ch:g11:circuits-and-power}).
\end{proposition}

\begin{proof}
Current is charge delivered per second
(\cref{def:g11:circuits-and-power:current}): over a shrinking interval,
$i$ is the derivative of $q$; and $q = Cu$ with $C$ constant gives
$C\,du/dt$.
\end{proof}

\begin{remark}[Two consequences]\label{rem:g12:rc-rl-circuits:continuity}
If $u$ is constant, $i = 0$: a charged \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} passes no steady current.
And $u$ can never jump --- a step would demand infinite current: the
\omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}'s \omterm{def:g11:circuits-and-power:voltage}{voltage} is the circuit's memory.
\end{remark}

\section{Charging a capacitor: the RC circuit}

One loop --- source $\mathcal{E}$ (\cref{def:g11:circuits-and-power:emf}),
switch, resistor, empty \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}: close $K$ at $t = 0$.

\begin{omfigure}
\begin{circuitikz}[font=\small]
  \draw (0,0) to[battery1, l=$\mathcal{E}$] (0,2.4) to[nos, l=$K$] (2.1,2.4)
    to[R=$R$, i=$i$] (4.2,2.4) -- (5.4,2.4) to[C=$C$, v=$u$] (5.4,0) -- (0,0);
\end{circuitikz}

{\small The RC charging circuit: closing $K$ lets the source push charge
through $R$ onto the plates of $C$.}
\end{omfigure}

\begin{theorem}[Charging law of the RC circuit]\label{thm:g12:rc-rl-circuits:charging}
\index{RC circuit}
After the switch closes, the \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} \omterm{def:g11:circuits-and-power:voltage}{voltage} obeys, then follows from
$u(0) = 0$:
\[
RC\,\frac{du}{dt} + u = \mathcal{E},
\qquad
u(t) = \mathcal{E}\left(1 - e^{-t/\tau}\right),
\qquad \tau = RC .
\]
\end{theorem}

\begin{proof}
\omterm{def:g11:circuits-and-power:voltage}{Voltages} along the loop add (\cref{prop:g11:circuits-and-power:laws}):
$\mathcal{E} = u_R + u = Ri + u$ at every instant; substitute
$i = C\,du/dt$ (\cref{prop:g12:rc-rl-circuits:cdudt}). Now \emph{verify}
the candidate: $du/dt = (\mathcal{E}/\tau)e^{-t/\tau}$, so
$RC\,(\mathcal{E}/RC)\,e^{-t/\tau} + \mathcal{E}(1 - e^{-t/\tau}) =
\mathcal{E}$, true for all $t$; and $u(0) = \mathcal{E}(1-1) = 0$. That
no \emph{other} curve fits the equation and the start is proved in this
year's mathematics course on differential equations.
\end{proof}

\begin{definition}[Time constant]\label{def:g12:rc-rl-circuits:tau}
The \emph{time constant}\index{time constant} of an \omterm{thm:g12:rc-rl-circuits:charging}{RC circuit} is
$\tau = RC$. \omterm{def:g10:orders-of-magnitude:unit}{Units}: $\unit{\ohm} \times \unit{F} =
(\unit{V/A})(\unit{C/V}) = \unit{C/A} = \unit{s}$ --- the circuit's
own tempo.
\end{definition}

\begin{method}[Reading $\tau$ on the curve]\label{met:g12:rc-rl-circuits:reading}
Three equivalent readings, straight off a graph:
\begin{enumerate}
  \item \emph{tangent at the origin:} $u'(0) = \mathcal{E}/\tau$, so
        the initial tangent reaches the asymptote at $t = \tau$;
  \item \emph{63\%:} $u(\tau) = \mathcal{E}(1 - e^{-1}) \approx
        0.63\,\mathcal{E}$;
  \item \emph{95\%:} $u(3\tau) \approx 0.95\,\mathcal{E}$ --- charged,
        for practical purposes ($5\tau$: $99\%$).
\end{enumerate}
\end{method}

\begin{proposition}[Discharge]\label{prop:g12:rc-rl-circuits:discharge}
A \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} charged to $U_0$ and closed on a resistor $R$ alone obeys
$RC\,du/dt + u = 0$ and follows
\[
u(t) = U_0\,e^{-t/\tau}, \qquad \tau = RC
\]
--- $37\%$ left at $\tau$, $5\%$ at $3\tau$.
\end{proposition}

\begin{proof}
Same loop law, no source; substitution again:
$RC(-U_0/\tau)e^{-t/\tau} + U_0 e^{-t/\tau} = 0$, with $u(0) = U_0$.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis, width=9.2cm, height=5.4cm, xlabel={$t$}, ylabel={$u$},
    xmin=0, xmax=5, ymin=0, ymax=7.4, xtick={0,1,2,3,4,5},
    xticklabels={$0$,$\tau$,$2\tau$,$3\tau$,$4\tau$,$5\tau$},
    ytick={0,2.21,3.79,6.0},
    yticklabels={$0$,$0.37\,\mathcal{E}$,$0.63\,\mathcal{E}$,$\mathcal{E}$}]
  \addplot[omExo, dashed, domain=0:5, samples=2] {6};
  \addplot[omExo, dashed, domain=0:1.18, samples=2] {6*x};
  \addplot[omExo, dashed, domain=0:1.1, samples=2] {6-6*x};
  \addplot[omDef, thick, domain=0:5, samples=80] {6*(1-exp(-x))};
  \addplot[omThm, thick, domain=0:5, samples=80] {6*exp(-x)};
  \draw[dashed, omExo] (1,0) -- (1,3.79) -- (0,3.79);
  \draw[dashed, omExo] (3,0) -- (3,5.70);
\end{axis}
\end{tikzpicture}

{\small Charge (blue, from $u = 0$ toward $\mathcal{E}$) and discharge
(red, from $U_0 = \mathcal{E}$). Both initial tangents meet their
asymptote at $t = \tau$; $63\%$ done at $\tau$, $95\%$ at $3\tau$.}
\end{omfigure}

\begin{example}[A flash unit's numbers]\label{ex:g12:rc-rl-circuits:flashrc}
A photo flash charges $C = \qty{800}{\micro F}$ through
$R = \qty{1.0}{k\ohm}$: $\tau = 10^3 \times 8.0\times10^{-4} =
\qty{0.80}{s}$, so the ``ready'' light --- wired to trip at $95\%$ ---
waits $3\tau \approx \qty{2.4}{s}$: the whine of the hook. Fired
through the flash tube's mere \qty{0.50}{\ohm}, the \emph{same}
\omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} empties with $\tau' = \qty{0.40}{ms}$: two thousand times
faster out than in.
\end{example}

\section{The coil: inductance and the RL circuit}

\begin{definition}[Inductor and inductance]\label{def:g12:rc-rl-circuits:inductor}
An \emph{inductor}\index{inductor} is a \omterm{def:g11:magnetic-fields:solenoid}{coil} of wire: its current
threads its own turns with a \omterm{def:g11:magnetic-fields:bfield}{magnetic field}
(\cref{ch:g11:magnetic-fields}); whenever it \emph{changes}, a \omterm{def:g11:circuits-and-power:voltage}{voltage}
appears across the \omterm{def:g11:magnetic-fields:solenoid}{coil}:
\[
u = L\,\frac{di}{dt} .
\]
The constant $L$ is the \emph{inductance}\index{inductance}, in
\emph{henries}\index{henry} (\unit{H}): $\qty{1}{H} = \qty{1}{V.s/A}$.
\end{definition}
\admitted

\begin{remark}[The coil resists change]\label{rem:g12:rc-rl-circuits:oppose}
Why a changing current makes a \omterm{def:g11:circuits-and-power:voltage}{voltage} --- induction --- is derived in
the university volumes; here we take the law and its character: the
\omterm{def:g11:magnetic-fields:solenoid}{coil} \emph{opposes changes} of its current. Mirror twins: a \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}'s
$u$ cannot jump, a \omterm{def:g11:magnetic-fields:solenoid}{coil}'s $i$ cannot jump.
\end{remark}

\begin{omfigure}
\begin{circuitikz}[font=\small]
  \draw (0,0) to[battery1, l=$\mathcal{E}$] (0,2.4) to[nos, l=$K$] (2.1,2.4)
    to[R=$R$] (4.2,2.4) -- (5.4,2.4) to[L=$L$, i=$i$, v=$u$] (5.4,0) -- (0,0);
\end{circuitikz}

{\small The \omterm{prop:g12:rc-rl-circuits:rl}{RL circuit}: after $K$ closes, the \omterm{def:g11:magnetic-fields:solenoid}{coil} lets the current
climb only at its own pace $\tau = L/R$.}
\end{omfigure}

\begin{proposition}[Current rise in an RL circuit]\label{prop:g12:rc-rl-circuits:rl}
\index{RL circuit}
Closing the switch on a source $\mathcal{E}$, a resistor $R$ and a \omterm{def:g11:magnetic-fields:solenoid}{coil}
$L$ in series gives $\mathcal{E} = Ri + L\,di/dt$, i.e.
\[
\frac{L}{R}\,\frac{di}{dt} + i = \frac{\mathcal{E}}{R},
\qquad\text{solved by}\quad
i(t) = \frac{\mathcal{E}}{R}\left(1 - e^{-t/\tau}\right),
\quad \tau = \frac{L}{R} .
\]
\end{proposition}

\begin{proof}
The loop law again; and it is the \emph{same equation} as in
\cref{thm:g12:rc-rl-circuits:charging} with $u \to i$,
$\mathcal{E} \to \mathcal{E}/R$, $RC \to L/R$ --- the same substitution
verifies the solution, and every reading of
\cref{met:g12:rc-rl-circuits:reading} transfers.
\end{proof}

\begin{example}[An electromagnet wakes up]\label{ex:g12:rc-rl-circuits:rl}
A relay \omterm{def:g11:magnetic-fields:solenoid}{coil}, $L = \qty{0.30}{H}$ and $R = \qty{6.0}{\ohm}$, on a
\qty{12}{V} supply: final current $\mathcal{E}/R = \qty{2.0}{A}$, at
the pace $\tau = 0.30/6.0 = \qty{50}{ms}$ --- awake in $3\tau \approx
\qty{0.15}{s}$.
\end{example}

\begin{remark}[The spark at opening]\label{rem:g12:rc-rl-circuits:spark}
Open a switch on a live \omterm{def:g11:magnetic-fields:solenoid}{coil} and $i$ must crash to zero in a fraction
of a millisecond: $di/dt$ is huge and $u = L\,di/dt$ reaches hundreds
or thousands of \omterm{def:g11:circuits-and-power:voltage}{volts} --- a spark jumps the contact. A car's ignition
\omterm{def:g11:magnetic-fields:solenoid}{coil} does it on purpose, thousands of times a minute.
\end{remark}

\section{Stored energy}

\begin{proposition}[Energy in a capacitor, energy in a coil]\label{prop:g12:rc-rl-circuits:energy}
\index{energy!of a capacitor}\index{energy!of a coil}
A \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} $C$ charged to \omterm{def:g11:circuits-and-power:voltage}{voltage} $u$ and a \omterm{def:g11:magnetic-fields:solenoid}{coil} $L$ carrying a current
$i$ store
\[
E_C = \tfrac12\,C u^2 = \frac{q^2}{2C},
\qquad
E_L = \tfrac12\,L i^2 .
\]
\end{proposition}

\begin{proof}
Landing a small charge $dq$ on plates already at \omterm{def:g11:circuits-and-power:voltage}{voltage} $u$ costs
$u\,dq$ (\cref{def:g11:circuits-and-power:voltage}); stacking the
costs, the total is the area under the line $u = q/C$ --- the triangle
below --- so $E_C = \tfrac12 qu = \tfrac12 Cu^2$. The same triangle for
the \omterm{def:g11:magnetic-fields:solenoid}{coil} (raising $i$ by $di$ costs $Li\,di$) gives
$E_L = \tfrac12 Li^2$.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}[font=\small, x=1.35cm, y=1.05cm]
  \fill[omDef!25] (0,0) -- (4,0) -- (4,2.6) -- cycle;
  \fill[omDef!60] (2.4,0) rectangle (2.7,1.658);
  \draw[omDef, thick] (0,0) -- (4,2.6);
  \draw[-Stealth] (0,0) -- (4.8,0) node[right] {$q$};
  \draw[-Stealth] (0,0) -- (0,3.1) node[above] {$u = q/C$};
  \draw[dashed] (4,0) -- (4,2.6) -- (0,2.6);
  \node[below] at (4,0) {$q_0$}; \node[left] at (0,2.6) {$u_0$};
  \node[below, font=\footnotesize] at (2.55,0) {$dq$};
  \node[omDef!50!black, font=\footnotesize] at (2.9,0.85)
    {$E_C$};
\end{tikzpicture}

{\small Each slice $dq$ costs $u\,dq$; the whole charge costs the
triangle's area $E_C = \tfrac12 q_0 u_0$ --- the early \omterm{def:g11:fundamental-interactions:charge}{coulombs} board
at low \omterm{def:g11:circuits-and-power:voltage}{voltage}.}
\end{omfigure}

\begin{example}[Energy orders of magnitude]\label{ex:g12:rc-rl-circuits:energies}
The flash \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} of \cref{ex:g12:rc-rl-circuits:flashrc}:
$\tfrac12 \times 8.0\times10^{-4} \times 300^2 = \qty{36}{J}$. A
\qty{3000}{F} supercapacitor at \qty{2.7}{V}: \qty{1.1e4}{J} --- an AA
battery's worth. A \qty{10}{mH} \omterm{def:g11:magnetic-fields:solenoid}{coil} at \qty{10}{A}: \qty{0.50}{J}.
Less than batteries hold --- but releasable in a millisecond.
\end{example}

\begin{remark}[What the two time-keepers are for]\label{rem:g12:rc-rl-circuits:applications}
\index{smoothing}\index{timing circuit}
Slow-in, fast-out \omterm{def:g11:work-of-force:power}{powers} the camera flash and the defibrillator. The
\omterm{def:g12:mechanical-waves:delay}{delay} $\tau = RC$ runs \omterm{rem:g12:rc-rl-circuits:applications}{timing circuits}: intermittent wipers, blinking
beacons, stairwell lights. A \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} across a supply \emph{smooths}
\omterm{def:g11:circuits-and-power:voltage}{voltage} ($u$ cannot jump); a series \omterm{def:g11:magnetic-fields:solenoid}{coil} smooths current ($i$ cannot
jump). And \omterm{def:g11:magnetic-fields:solenoid}{coil} plus \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} together make an \omterm{def:g12:oscillators-and-time:oscillator}{oscillator}
(\cref{ch:g12:rlc-oscillations}).
\end{remark}

\section{Exercises}

\begin{exercise}[$\star$]\label{exo:g12:rc-rl-circuits:1}
A \qty{470}{\micro F} \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} is charged to \qty{12}{V}: what charge does
it hold? What \omterm{def:g11:circuits-and-power:voltage}{voltage} would put \qty{2.0}{mC} on \qty{100}{\micro F}?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:rc-rl-circuits:2}
Attribute \qty{47}{pF}, \qty{2200}{\micro F} and \qty{10}{F} among a
power-supply smoother, a radio tuner and a supercapacitor; compute the last
one's \omterm{def:g10:energy-conservation:energy}{energy} at \qty{2.7}{V}.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:rc-rl-circuits:3}
The \omterm{def:g11:circuits-and-power:voltage}{voltage} across a \qty{220}{\micro F} \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} rises steadily from $0$
to \qty{5.0}{V} in \qty{10}{ms}: what current flows in? What current once
the \omterm{def:g11:circuits-and-power:voltage}{voltage} holds still, and why?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:rc-rl-circuits:4}
Compute $\tau$ for $R = \qty{10}{k\ohm}$, $C = \qty{100}{\micro F}$, then
for $L = \qty{0.10}{H}$, $R = \qty{50}{\ohm}$. Check, \omterm{def:g10:orders-of-magnitude:unit}{unit} by \omterm{def:g10:orders-of-magnitude:unit}{unit}, that
both combinations are seconds.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:rc-rl-circuits:5}
A \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} charges toward \qty{6.0}{V}, passing \qty{3.8}{V} at
$t = \qty{2.0}{s}$. Read off the \omterm{def:g12:rc-rl-circuits:tau}{time constant}; when is the charge $95\%$ done?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:6}
An \omterm{thm:g12:rc-rl-circuits:charging}{RC circuit}: $\mathcal{E} = \qty{9.0}{V}$, $R = \qty{4.7}{k\ohm}$,
$C = \qty{220}{\micro F}$. Compute $\tau$, $u(\tau)$, $u(3\tau)$, and the
current at the instant the switch closes.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:7}
Verify by substitution that $u(t) = \mathcal{E}(1 - e^{-t/RC})$
satisfies $RC\,du/dt + u = \mathcal{E}$; what do its value at $t = 0$
and its large-$t$ limit encode physically?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:8}
A \qty{100}{\micro F} \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} charged to \qty{12}{V} discharges through
\qty{47}{k\ohm}: compute $\tau$, $u(\tau)$, and the time for $u$ to drop
below \qty{0.60}{V}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:9}
The current in a \qty{0.50}{H} \omterm{def:g11:magnetic-fields:solenoid}{coil} climbs steadily from $0$ to
\qty{2.0}{A} in \qty{40}{ms}: what \omterm{def:g11:circuits-and-power:voltage}{voltage} appears across it? With
$u = L\,di/dt$, why does \emph{opening} a switch on a \omterm{def:g11:magnetic-fields:solenoid}{coil} spark, not closing?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:10}
The relay of \cref{ex:g12:rc-rl-circuits:rl}: verify by substitution that
$i(t) = (\mathcal{E}/R)(1 - e^{-Rt/L})$ satisfies
$L\,di/dt + Ri = \mathcal{E}$, then compute $i(\tau)$ and $i(3\tau)$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:rc-rl-circuits:11}
Show that $u'(0) = \mathcal{E}/\tau$, and deduce that the tangent at
the origin meets the asymptote at $t = \tau$. On a recorded curve that
gives $\tau = \qty{0.50}{s}$, with $R = \qty{2.0}{k\ohm}$: find $C$.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:rc-rl-circuits:12}
Rank by stored \omterm{def:g10:energy-conservation:energy}{energy}, computing each: a \qty{3000}{F} supercapacitor
at \qty{2.7}{V}; a flash \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}, \qty{800}{\micro F} at
\qty{300}{V}; a \qty{10}{mH} \omterm{def:g11:magnetic-fields:solenoid}{coil} at \qty{10}{A}; an AA battery
($E \approx \qty{1.3e4}{J}$). What do the first three offer that the
battery cannot?
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:rc-rl-circuits:13}
A stairwell timer lights while $u < 0.63\,\mathcal{E}$ across its
charging \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}, going dark at $t = \tau$. Design the light to last
\qty{5.0}{s} using $C = \qty{100}{\micro F}$: what $R$? For a threshold
$0.50\,\mathcal{E}$, show the \omterm{def:g12:mechanical-waves:delay}{delay} is $\tau \ln 2$ and compute it.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:rc-rl-circuits:14}
A \qty{2200}{\micro F} \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} smooths a supply: between recharges,
it alone feeds a load drawing \qty{0.50}{A} for \qty{10}{ms}. From
$i = C\,du/dt$, compute the \omterm{def:g11:circuits-and-power:voltage}{voltage} droop, then the \omterm{def:g12:rc-rl-circuits:capacitor}{capacitance}
keeping it under \qty{0.50}{V}.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:rc-rl-circuits:15}
An ignition \omterm{def:g11:magnetic-fields:solenoid}{coil}, $L = \qty{0.80}{H}$, carries \qty{2.0}{A} when the
breaker cuts it off in about \qty{1.0}{ms}. Estimate the \omterm{def:g11:magnetic-fields:solenoid}{coil} \omterm{def:g11:circuits-and-power:voltage}{voltage}
during the cut and the spark's \omterm{def:g10:energy-conservation:energy}{energy}; why does a slow interruption
give no spark?
\end{exercise}

\section{Problem: The camera flash}

\begin{problem}[{Weekend problem --- the camera flash: a two-second
whine, a millisecond of glory, and the wall-mounted cousin that restarts
hearts --- one exponential runs them all}]
\label{pb:g12:rc-rl-circuits:1}
A compact flash \omterm{def:g10:orders-of-magnitude:unit}{unit} stores \omterm{def:g10:energy-conservation:energy}{energy} in a \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}
$C = \qty{800}{\micro F}$, charged through $R = \qty{1.0}{k\ohm}$ from
a \omterm{def:g10:energy-conservation:transfer}{converter} modeled as an ideal source $\mathcal{E} = \qty{300}{V}$;
the ``ready'' light trips at $95\%$ of full \omterm{def:g11:circuits-and-power:voltage}{voltage}. Fired, the
\omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} sees only the flash tube, $R_f = \qty{0.50}{\ohm}$. Data: an
AA cell delivers \qty{1.5}{V} and \qty{2.4}{A.h}; $e = \qty{1.60e-19}{C}$.

\medskip
\noindent\textbf{Part I --- The reservoir.}
\begin{enumerate}
  \item Compute the charge on the \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor} at \qty{300}{V}.
  \item How many \omterm{def:g11:fundamental-interactions:elementary}{elementary charges} is that?
  \item Compute the stored \omterm{def:g10:energy-conservation:energy}{energy} $E_C$.
  \item The AA cell holds $q = It \approx \qty{8.6e3}{C}$, hence about
        \qty{1.3e4}{J}. How many times the \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}'s \omterm{def:g10:energy-conservation:energy}{energy} is that?
  \item So why bother with the \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}? One sentence, using ``\omterm{def:g11:work-of-force:power}{power}''.
\end{enumerate}

\medskip
\noindent\textbf{Part II --- The wait for green.}
\begin{enumerate}[resume]
  \item Write the loop law and turn it into the differential equation
        for $u(t)$.
  \item Verify by substitution that $u(t) = \mathcal{E}(1 - e^{-t/RC})$
        solves it, starting from $u(0) = 0$.
  \item Compute the \omterm{def:g12:rc-rl-circuits:tau}{time constant} $\tau$.
  \item After what time does the ready light turn green, and at what
        \omterm{def:g11:circuits-and-power:voltage}{voltage}? Recognize the two-second whine.
  \item Compute the initial charging current; why does it then die
        away?
\end{enumerate}

\medskip
\noindent\textbf{Part III --- The millisecond of glory.}
\begin{enumerate}[resume]
  \item Write the discharge law $u(t)$ through the tube and compute the
        new \omterm{def:g12:rc-rl-circuits:tau}{time constant} $\tau'$.
  \item Compute the peak current through the tube.
  \item Compute the peak \omterm{def:g11:work-of-force:power}{power}; compare it with a \qty{2.2}{kW} kettle.
  \item Taking the flash as over at $3\tau'$, give its duration and
        average \omterm{def:g11:work-of-force:power}{power}.
  \item Form the ratio of charging time to flash time: same \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor},
        same charge --- what changed, and what does that make a
        \omterm{def:g12:rc-rl-circuits:capacitor}{capacitor}: an \omterm{def:g10:energy-conservation:energy}{energy} tank, or a \omterm{def:g11:work-of-force:power}{power} lever?
\end{enumerate}

\medskip
\noindent\textbf{Part IV --- The cousin on the wall.}
A defibrillator must deliver $E_C = \qty{200}{J}$ at
$U_0 = \qty{1500}{V}$; chest and paddles present about \qty{50}{\ohm}.
\begin{enumerate}[resume]
  \item Compute the required \omterm{def:g12:rc-rl-circuits:capacitor}{capacitance}.
  \item Compute the stored charge.
  \item Compute the shock's \omterm{def:g12:rc-rl-circuits:tau}{time constant} and duration ($3\tau$);
        compare with the flash.
  \item Compute the peak current and peak \omterm{def:g11:work-of-force:power}{power} in the patient.
  \item The recharge circuit must make the device ready ($95\%$) in
        \qty{6.0}{s}: find the required charging \omterm{def:g11:circuits-and-power:resistance}{resistance} and the
        initial charging current --- the numbers a designer would order.
\end{enumerate}
\end{problem}
